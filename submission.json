{
    "config": ["config/xlsdk_address.json", "config/alsdk_address.json"],  # paths of at least 2 config files
    "build": "run.py",
    "library": ["src"],
    "notebook": ["checkpoint2.ipynb"] # path to at least one notebook meant for reading by someone other than you.
    "targets": ["xlsdk", "alsdk"] # at least two targets
}
